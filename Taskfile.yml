version: "3"

tasks:
  build:
    desc: Generate static HTML (i18n)
    cmds:
      - go run build.go

  docker-build:
    desc: Build production container image
    cmds:
      - docker build -t meshchatx-website:latest .
